<div class="cajas2 formedgroup forma mb45px">
  <%= simple_form_for @group do |f| %>
        <div><h3>Editar Grupo</h3></div>
        <%= f.hidden_field :id %>
        Imágen
        <div class="stylebut">
          cambiar Foto
          <div class="wrapperbut">
            <%= f.file_field :logo %>
          </div>
        </div>
        <%= f.input :name %>
        Email
        <%= f.email_field :email, :label =>'Email' %>
        <%= f.input :link %>
        <%= f.input :description, :class=> "textarea2" %>
        <%= f.submit "Guardar", :class=> "sugspp nobtn" %>
  <% end %>
</div>